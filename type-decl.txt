union IntList begin
  Empty[];
  Node[Int, IntList]
end

record PairOfLists begin
  left: IntList;
  right: IntList
end

func main(): Unit begin
  let u: IntList = Empty[] in
  let p: PairOfLists = PairOfLists { left=u, right=u } in
  let v: IntList = p.right in
  match v begin
    Empty[] -> ()
    Node[a, b] -> ()
  end
end
