union MyUnion begin
  Owned[Promise*(Int)];
  Normal[Promise(Int)]
end

func main(): Unit begin
  let p: Promise*(Int) = promise Int in
  match Owned[p] begin
    Normal[n] -> ()
    Owned[o] -> (o <- 5)
  end
end
